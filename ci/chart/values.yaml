domain:

deploySecrets: true

envKey: Production
apiRoot: /api
imageTag:
ReCaptchaOptions:
  SiteKey: # secret
  Enabled: true

image:
  repository: ghcr.io/tourmalinecore/inner-circle/dev-auth-ui
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag:

imageCredentials:
  registry: ghcr.io
  username: # secret
  password: # secret